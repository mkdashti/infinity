CUDA_DIR = /usr/local/cuda-6.0

CUDA_LIB_DIR := $(CUDA_DIR)/lib64
CUDA_ARCH_FLAGS := -arch=sm_30
CC_FLAGS += $(CUDA_ARCH_FLAGS) -g -G

CC := $(CUDA_DIR)/bin/nvcc

EXE = bpt

all: $(EXE)

% : %.cu
	$(CC) $< $(CC_FLAGS) $(LIB_FLAGS) -o $@

clean: 
	rm -f $(EXE)

